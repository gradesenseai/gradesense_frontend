# GradeSense Frontend (Vercel)

- Brand: Inter font, Green #00C774, Black #000000, White #FFFFFF, Gray #EDEDED.
- Requires backend API base URL.

## Environment

Create a Vercel project with root `/frontend` and set:
